table.table.table-hover
    thead
        tr
            th ID
            th FIRST NAME
            th LAST NAME
            th EMAIL
    tbody
        - for (var i = 0; i < users.length; i++)
            tr
                td= users[i].id
                td= users[i].first
                td= users[i].last
                td= users[i].email